# ─────────────────────────────────────────────────────────────────
# Agency OS — Environment Variables
# Copy to .env.local and fill in your values
# NEVER commit .env.local to version control
# ─────────────────────────────────────────────────────────────────

# DATABASE
DATABASE_URL="postgresql://neondb_owner:npg_XN7KywE0lsfk@ep-holy-math-av7tqaw0-pooler.c-11.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# NEXTAUTH / AUTH.JS
# Generate with: openssl rand -base64 32
AUTH_SECRET="iwlN9b1T0oTaK1HReQPZKxn3yUMukC2b"
AUTH_URL="http://localhost:3000"

# EMAIL (choose one provider)
# Option 1: SMTP
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="your-app-password"
EMAIL_FROM="Agency OS <noreply@youragency.com>"

# Option 2: Resend
RESEND_API_KEY=""

# Option 3: Amazon SES
AWS_SES_REGION=""
AWS_SES_ACCESS_KEY_ID=""
AWS_SES_SECRET_ACCESS_KEY=""

# FILE STORAGE
# Option 1: Local (development)
STORAGE_PROVIDER="local"
STORAGE_LOCAL_PATH="./public/uploads"

# Option 2: AWS S3 / S3-compatible
STORAGE_PROVIDER="s3"
AWS_S3_BUCKET=""
AWS_S3_REGION=""
AWS_S3_ACCESS_KEY_ID=""
AWS_S3_SECRET_ACCESS_KEY=""
AWS_S3_ENDPOINT=""  # for S3-compatible providers like Cloudflare R2

# NEXT_PUBLIC (visible to browser - keep minimal)
NEXT_PUBLIC_APP_NAME="Agency OS"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
